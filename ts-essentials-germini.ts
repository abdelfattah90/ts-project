/*
 * تقرير الخبير: المفاهيم الأساسية لـ TypeScript في تطوير Angular
 * مُقدم من: Technical Instructor (Senior Level)
 * التاريخ: 2024-10-29
 *
 * هذا الدليل التعليمي يركز على تقديم شرح منظم ومفصل لأهم 10 مفاهيم في لغة TypeScript،
 * مع تسليط الضوء على كيفية تطبيقها وأهميتها المعمارية في سياق مشاريع Angular الحديثة.
 * تم تصميم الشرح ليكون بسيطاً وخالياً من التعقيد المتقدم، ليرسخ الفهم العملي.
 */

/*
 * ====================================================================
 * الفهرس (Index)
 * ====================================================================
 */
/*
1️⃣ Type System: Basic types, interfaces, and type aliases
2️⃣ Classes: Properties, methods, constructors, and inheritance
3️⃣ Access Modifiers: Public, private, protected, and readonly
4️⃣ Decorators: Understanding metadata and decorator patterns
5️⃣ Generics: Creating reusable and type-safe components
6️⃣ Advanced Types: Union types, intersection types, and type guards
7️⃣ Async Patterns: Promises, async/await, and error handling
8️⃣ ES6+ Features: Arrow functions, destructuring, spread operators
9️⃣ Modules: Import/export and module organization
🔟 TypeScript Configuration: Understanding tsconfig.json settings
*/

// ====================================================================
// 1️⃣ Type System: Basic types, interfaces, and type aliases
// ====================================================================

/*
 * النوع (Type System) هو حجر الزاوية الذي تبنى عليه سلامة شيفرة TypeScript.
 * هو ما يمكّن المترجم (Compiler) من التحقق من صحة استخدام البيانات قبل التشغيل.
 *
 * 1. الأنواع الأساسية (Basic Types):
 * تشمل الأنواع البسيطة مثل: 'string', 'number', 'boolean', 'void', بالإضافة إلى الأنواع الأكثر مرونة مثل 'any' و 'unknown'. في Angular، يُنصح بتجنب استخدام 'any' قدر الإمكان للحفاظ على فائدة TypeScript.[1]
 *
 * 2. الواجهات (Interfaces):
 * تُستخدم الـ Interfaces لتعريف شكل الكائنات (Object Shapes) أو لتعريف عقد (Contract) يجب على كلاس معين تنفيذه باستخدام الكلمة المفتاحية 'implements'.[2, 3] الـ Interfaces تدعم الوراثة الهيكلية عبر 'extends'.[4]
 *
 * 3. الأسماء المستعارة للأنواع (Type Aliases):
 * تُعلن باستخدام الكلمة المفتاحية 'type'. وهي تتميز بمرونتها العالية؛ حيث يمكنها تمثيل أي نوع، بما في ذلك الأنواع الأولية، أو الأنواع المركبة مثل أنواع الاتحاد (Union Types) وأنواع التقاطع (Intersection Types) والأنواع المشروطة.[4]
 *
 * 💡 الموازنة بين Interface و Type Alias:
 * عند تطوير تطبيقات Angular، يجب ترجيح كفة المرونة الافتراضية. يوصى باستخدام 'type' بشكل افتراضي لأنه يمنح القدرة على تعريف Union Types (وهو غير ممكن مع الواجهات) ويمنع السلوك غير المرغوب فيه لـ "دمج التصريحات" (Declaration Merging) الذي يحدث في Interfaces.[4]
 * ومع ذلك، يظل استخدام 'interface' ضرورياً عندما تحتاج إلى تعريف عقد يجب أن يُنفذ بواسطة كلاسات Angular (مثل Services أو Components) عبر 'implements'.[3] كما أن استخدام 'extends' في الواجهات قد يوفر أداء فحص أسرع قليلاً في حالات الوراثة الهرمية الكبيرة.[4]
 */
let userName: string = "Angular Developer";
let appVersion: number = 17.0;

interface UserData {
    id: number;
    email: string;
}

// مثال على استخدام Type Aliases لـ Union Type
type StatusCode = 200 | 404;
type ProfileInfo = UserData & {
    lastLogin: Date; // Intersection Type
};

// ====================================================================
// 2️⃣ Classes: Properties, methods, constructors, and inheritance
// ====================================================================

/*
 * الكلاسات هي الهيكل التنظيمي الأساسي لشيفرة Angular، حيث يتم تعريف كل من المكونات (Components)، الخدمات (Services)، والتوجيهات (Directives) ككلاسات.[3]
 *
 * 1. الخصائص والميثودات:
 * يتم تعريف الخصائص لتخزين حالة الكائن، بينما الميثودات تحدد سلوك الكلاس.
 *
 * 2. الباني (Constructor) وحقن التبعيات (DI):
 * الباني هو المسؤول عن تهيئة الكلاس واستقبال التبعيات (Dependencies). يُستخدم الباني في Angular لتنفيذ حقن التبعيات (DI)، مما يسمح للمكونات والخدمات بطلب مثيلات كلاسات أخرى.
 *
 * 3. الوراثة (Inheritance):
 * يمكن للكلاسات المشتقة استخدام 'extends' للوراثة من كلاس أساسي. إذا احتوى الكلاس المشتق على باني، يجب عليه استدعاء 'super()' أولاً لتمرير أي حجج مطلوبة إلى باني الكلاس الأب.[5] هذه الآلية مفيدة لإنشاء كلاسات أساسية تحتوي على منطق مشترك (مثل BaseService).
 *
 * 💡 اختصار الباني (Constructor Shorthand):
 * يوفر TypeScript اختصاراً يقلل من الكود المكرر (DRY Principle) عند تعريف الخصائص. يمكن تعريف الخصائص وتعيينها مباشرة داخل الباني باستخدام معدلات الوصول (مثل 'private')، مما يلغي الحاجة إلى تصريح الخاصية مرتين.[5] يتم تطبيق هذا بشكل مكثف في حقن التبعيات في Angular: 'constructor(private http: HttpClient)'.
 */
class BaseService {
    // خاصية تتم تهيئتها عند إنشاء الكلاس
    protected apiUrl: string = 'api/v1/';

    // يستخدم هذا الميثود في الكلاسات المشتقة
    protected logRequest(endpoint: string): void {
        console.log(`Requesting: ${this.apiUrl}${endpoint}`);
    }
}

class ProductService extends BaseService {
    // استخدام اختصار الباني لتعريف وحقن التبعية (الافتراضية)
    constructor(private serviceName: string) {
        super(); // استدعاء باني الكلاس الأب
        this.logRequest(this.serviceName);
    }

    getProduct(id: number): string {
        return `Product ID: ${id}`;
    }
}

// ====================================================================
// 3️⃣ Access Modifiers: Public, private, protected, and readonly
// ====================================================================

/*
 * معدلات الوصول تُستخدم لضبط رؤية أعضاء الكلاسات، وهي بالغة الأهمية في هندسة تطبيقات Angular لتحديد حدود التغليف (Encapsulation).
 *
 * 1. Public: هو المعدّل الافتراضي. يمكن الوصول إلى العضو المصرح كـ 'public' من أي مكان، ويُعتبر جزءاً من الواجهة البرمجية العامة للكلاس.[3]
 *
 * 2. Private: يحد الوصول إلى داخل الكلاس الذي صرح به فقط. يستخدم هذا لعزل تفاصيل التنفيذ الداخلية.
 *
 * 3. Protected: يسمح بالوصول من داخل الكلاس نفسه ومن الكلاسات التي ترث منه (Derived Classes).[3]
 *
 * 4. Readonly: يفرض الثبات (Immutability). يمكن تهيئة الخاصية إما عند التصريح بها أو داخل الباني فقط، ويمنع أي تعيين لاحق للقيمة خارج الباني.[3]
 *
 * 💡 دور Protected في Angular Style Guide:
 * توصي إرشادات تصميم Angular باستخدام 'protected' للأعضاء التي تستخدم فقط داخل قالب HTML الخاص بالمكون (Template).[6] هذا الإجراء يمنع هذه الأعضاء من أن تكون جزءاً من الواجهة البرمجية العامة (Public API) للمكون، مما يحافظ على نظافة الكلاس ويقلل من فرصة استهلاكها بطرق غير مقصودة عبر حقن التبعية أو الاستعلامات الخارجية.
 */
class LoggerComponent {
    // public: متاح للجميع (جزء من API)
    public logCount: number = 0;

    // private: غير متاح من خارج الكلاس
    private _hiddenState: string = "setup";

    // protected: متاح للقالب لكن ليس للوحدات الخارجية المحقونة [6]
    protected updateTemplate(): string {
        return `Count is ${this.logCount}`;
    }

    // readonly: قيمة ثابتة
    readonly configName: string = "SystemConfig";

    increment(): void {
        this.logCount++;
    }
}

/*
الجدول 1: استخدام معدلات الوصول في سياق Angular

| **المعدِّل** | **الوصول من داخل الكلاس** | **الوصول من الخارج (الـ Services)** | **الوصول من قالب Angular (Template)** |
|---|---|---|---|
| **`public`** | نعم | نعم (يحدد الواجهة العامة) | نعم |
| **`private`** | نعم | لا (للتفاصيل التنفيذية الداخلية) | نعم (لكن استخدام `protected` أفضل للقالب) |
| **`protected`** | نعم | لا | نعم (موصى به للأعضاء غير العامة المستخدمة في Template).[6] |
| **`readonly`** | نعم (في Constructor فقط) | لا يمكن إعادة التعيين خارج الباني.[3] | نعم (للقراءة فقط) |
*/

// ====================================================================
// 4️⃣ Decorators: Understanding metadata and decorator patterns
// ====================================================================

/*
 * الـ Decorators (المُزينات) هي تصريحات خاصة تُضاف إلى الكلاسات أو أعضائها (ميثودات، خصائص، إلخ) بصيغة '@expression'.[7] وظيفتها الأساسية هي إضافة بيانات وصفية (Metadata) أو تعديل سلوك العضو في وقت التشغيل.
 *
 * 1. الأهمية في Angular:
 * تعتمد Angular بشكل جذري على Decorators لتحديد دور كل كلاس. على سبيل المثال، `@Component` يخبر Angular بأن هذا الكلاس هو مكون، ويزوده بالبيانات اللازمة (مثل الـ selector والـ templateUrl) لبناء شجرة المكونات وتحديد كيفية تفاعله مع نظام كشف التغييرات.[7]
 *
 * 2. مصانع المزينات (Decorator Factories):
 * لتمرير إعدادات أو حجج إلى المزّين (كما في `@Component({ selector: 'app-root' })`)، يجب أن يتم تنفيذه كـ "مصنع مزينات" (Decorator Factory)، وهي دالة تُرجع دالة المزّين الفعلية.[7, 8]
 *
 * 💡 Decorators كآلية فصل الاهتمامات:
 * Decorators في TypeScript تعمل كجسر لفصل منطق الكلاس عن متطلبات النظام الفرعية. عندما يستخدم المطور `@Component`، فإن TypeScript يقوم بإضافة سجلات تعريفية للكلاس. تقرأ مكتبة الانعكاس (Reflection library) في Angular هذه السجلات في وقت البناء (AOT) أو التشغيل لتحديد كيفية التعامل مع هذا الكلاس.[7] هذا يمنح Angular القدرة على تعديل سلوك الكلاس (Meta-programming) دون الحاجة إلى تغيير شيفرة الكلاس الأصلية، مما يزيد من مرونة البنية.
 */

// 1. Decorator Factory example
function ServiceMetadata(version: string) {
    // الدالة الخارجية (المصنع) تستقبل الحجج
    return function (target: Function) {
        // الدالة الداخلية (المزين الفعلي) يتم استدعاؤها على الكلاس
        console.log(`Service ${target.name} initialized with version: ${version}`);
        // يمكن هنا إضافة خصائص أو تعديل سلوك الكلاس
    };
}

@ServiceMetadata("2.1.0")
class DataService {
    //...
}

// ====================================================================
// 5️⃣ Generics: Creating reusable and type-safe components
// ====================================================================

/*
 * الـ Generics (الأنواع العامة) هي أدوات تسمح بإنشاء كود قابل لإعادة الاستخدام يعمل على مجموعة متنوعة من الأنواع دون التضحية بالسلامة النوعية.[9] يتم استخدام متغير نوع (Type Variable) يرمز إليه عادةً بالرمز 'T'.
 *
 * 1. التطبيق:
 * يمكن تطبيق Generics على الدوال، الواجهات، والكلاسات. الاستخدام الأبرز في Angular هو مع 'HttpClient' لتحديد نوع البيانات المتوقعة من استجابات API (مثل: `HttpClient.get<User>(url)`).
 *
 * 2. القيود النوعية (Type Constraints):
 * يمكن تحديد متطلبات معينة على النوع 'T' باستخدام 'extends' (مثلاً: `T extends HasId`). هذا يسمح للمترجم بضمان أن النوع المُمرر يحتوي على الخصائص اللازمة للعمليات داخل الدالة.[9] كما يمكن استخدام `Key extends keyof Type` لضمان أن مفتاح خاصية معينة موجود بالفعل في الكائن.[9]
 *
 * 💡 بناء طبقة Services فعالة:
 * باستخدام Generics، يمكن بناء خدمات قاعدة (Base Services) معممة لإدارة عمليات CRUD (Create, Read, Update, Delete) المشتركة. فبدلاً من تكرار كود جلب البيانات لكل نموذج (User, Product)، يمكن إنشاء `BaseApiService<T>`، مما يضمن أن جميع الميثودات تعمل بشكل آمن نوعياً مع نموذج البيانات المحدد.[9] هذا يساهم في تقليل الكود المكرر ويزيد من إنتاجية المطورين.
 */

// واجهة تُستخدم كقيد نوعي لضمان وجود الخاصية 'length' لتجنب خطأ المترجم.
interface Lengthwise {
    length: number;
}

// 1. Generic Interface
interface CacheEntry<T> {
    value: T;
    timestamp: Date;
}

// 2. Generic Service Method
function processList<T extends Lengthwise>(items: T): T {
    console.log(`Processing list of length: ${items.length}`);
    return items;
}

// 3. Generic function with constraint
interface HasName {
    name: string;
}

function printName<T extends HasName>(item: T): void {
    console.log(item.name);
}

// ====================================================================
// 6️⃣ Advanced Types: Union types, intersection types, and type guards
// ====================================================================

/*
 * هذه المفاهيم تمنح المطورين القدرة على إنشاء أنواع معقدة وديناميكية.
 *
 * 1. Union Types (أنواع الاتحاد):
 * تستخدم الرمز `|` لتمثيل مجموعة من الأنواع الممكنة (A أو B). مفيدة جداً لتمثيل مدخلات المكونات التي قد تقبل أنواع بيانات متعددة.[4]
 *
 * 2. Intersection Types (أنواع التقاطع):
 * تستخدم الرمز `&` لدمج خصائص أنواع متعددة، لإنشاء نوع جديد يمتلك جميع خصائص الأنواع المدمجة (A و B معاً).[2]
 *
 * 3. حارسات الأنواع (Type Guards):
 * هي تعابير لغوية تُستخدم لتضييق النوع ضمن نطاق معين في وقت التشغيل (Runtime). هي أساس التعامل الآمن مع Union Types.[10]
 *
 * آليات Type Guard الرئيسية:
 * - 'typeof': لفحص الأنواع الأولية (string, number, boolean).[11]
 * - 'instanceof': لفحص الكلاسات.
 * - Type Predicates: دوال مخصصة لتمكين فحص الهياكل المعقدة (مثلاً: `function isErrorResponse(obj): obj is ErrorResponse`).[10]
 *
 * 💡 Type Guards لسلامة الكود (Null Checks):
 * في Angular، غالبًا ما تكون المتغيرات قابلة للـ Null (مثل `User | null`). الـ Type Guards (مثل `if (user!== null)`) هي الطريقة القياسية والآمنة لإزالة الـ Null من مسار التنفيذ. هذا يمنع المترجم من الإبلاغ عن أخطاء، ويتجنب أخطاء زمن التشغيل الناتجة عن محاولة الوصول إلى خصائص على قيمة `null`.[10] هذا الإجراء ضروري لفرض مبدأ السلامة النوعية (Type Safety) في الشيفرة.
 */
type InputType = string | number;

// Intersection Type: دمج كائن المستخدم مع خاصية تسجيل
type LoggedUser = UserData & {
    loginTime: Date;
};

// Type Guard Example
function printValue(val: InputType) {
    if (typeof val === 'number') {
        // ضمن هذا النطاق، النوع هو number، ويمكن استخدام ميثوداته
        console.log(`Numeric value: ${val.toExponential()}`);
    } else {
        console.log(`String value: ${val.trim()}`);
    }
}

// ====================================================================
// 7️⃣ Async Patterns: Promises, async/await, and error handling
// ====================================================================

/*
 * تُستخدم الـ Promises و 'async/await' للتعامل مع العمليات غير المتزامنة، خاصة في الـ Services عند إجراء طلبات HTTP.
 *
 * 1. Promises:
 * تمثل عملية غير مكتملة بعد، وتضمن إرجاع نتيجة (Resolve) أو خطأ (Reject) في المستقبل. يتم التعامل مع الأخطاء عبر ميثود `.catch()`.[12]
 *
 * 2. Async/Await:
 * هي طبقة من السكر النحوي مبنية فوق الـ Promises. تجعل الكود غير المتزامن يبدو متزامناً وخطياً، مما يعزز سهولة القراءة بشكل كبير.[12] يجب تعريف الدالة كـ 'async' لاستخدام 'await' داخلها.
 *
 * 3. معالجة الأخطاء:
 * مع 'async/await'، يتم استبدال سلسلة `.catch()` المعقدة بآلية `try/catch` المألوفة لمعالجة الأخطاء المرفوضة (Rejected Promises).[12]
 *
 * 💡 تحسين سهولة القراءة (Readability):
 * في الـ Services التي تتطلب تسلسلاً من العمليات غير المتزامنة المعتمدة على بعضها البعض، يؤدي استخدام Promises التقليدي إلى سلاسل متداخلة (Nested structures) صعبة القراءة.[12] استخدام 'async/await' يحول هذه التفاعلات إلى تدفق منطقي وواضح، حيث يتم تغليف جميع العمليات المتتابعة داخل كتلة `try/catch` واحدة لمعالجة الأخطاء بفعالية.
 */

// استخدام Async/Await في Service
async function fetchConfig(endpoint: string): Promise<string> {
    // يتم استخدام try/catch لمعالجة الأخطاء المرفوضة
    try {
        // افتراض وجود دالة promise-based
        const response = await fetch(endpoint).then(res => res.json());
        return response.data;
    } catch (error) {
        console.error("Async operation failed", error);
        // رفض الـ Promise بشكل نهائي
        throw new Error("Configuration loading failed.");
    }
}

/*
الجدول 2: مقارنة معالجة الأخطاء غير المتزامنة

| **الميزة** | **Promises (باستخدام.then())** | **Async/Await (بناءً على Try/Catch)** |
|---|---|---|
| **بنية الكود** | تسلسل (Chain) من `.then()` و `.catch()`.[12] | خطي، يشبه الكود المتزامن.[12] |
| **معالجة الأخطاء** | استخدام ميثود `.catch()` في نهاية السلسلة. | استخدام بنية `try {} catch (error) {}` المألوفة.[12] |
| **سهولة القراءة** | قد تكون السلاسل معقدة (Nested structures).[12] | سهلة القراءة والفهم بشكل كبير. |
*/

// ====================================================================
// 8️⃣ ES6+ Features: Arrow functions, destructuring, spread operators
// ====================================================================

/*
 * يوفر TypeScript دعماً كاملاً لميزات ECMAScript 2015 (ES6) والإصدارات الأحدث، مما يعزز نظافة الكود.
 *
 * 1. دوال الأسهم (Arrow Functions):
 * توفر بناء جملة مختصرة للدوال. الميزة الأكثر أهمية هي أنها تحل مشكلة ربط سياق 'this' داخل دوال الـ Callback، حيث تحافظ دالة السهم دائماً على السياق القاموسي للكلاس.[13] هذا حيوي داخل مشغلات RxJS (Operators) في Angular.
 *
 * 2. التفكيك (Destructuring):
 * طريقة مختصرة لاستخراج الخصائص من الكائنات أو القيم من المصفوفات.[13] هذا يجعل قراءة البيانات من استجابات API أو كائنات الحالة أكثر وضوحاً.
 *
 * 3. معامل النشر (Spread Operator -...):
 * يُستخدم لنسخ أو دمج عناصر المصفوفات والكائنات بشكل ضحل (Shallow copy).[13]
 *
 * 💡 فرض الثبات (Immutability Enforcement):
 * في Angular، خاصة في سياق إدارة الحالة أو تحديث مدخلات المكونات الفرعية، من الضروري الحفاظ على الثبات. استخدام معامل النشر لإنشاء كائن جديد أو مصفوفة جديدة قبل تطبيق التغييرات (مثلاً عند تحديث حالة Component) يمنع التعديل المباشر (Mutation)، مما يقلل من الآثار الجانبية ويسهل على نظام كشف التغييرات في Angular اكتشاف التحديثات بكفاءة.[13]
 */
// Arrow function preserving 'this' context
class ComponentState {
    private componentId: number = 42;

    updateState(newValue: any) {
        // استخدام دالة سهم لضمان أن this يشير إلى ComponentState
        setTimeout(() => {
            console.log(`Updating state for component: ${this.componentId}`);
        }, 0);
    }
}

// Destructuring
const userSettings = { theme: 'dark', notifications: true };
const { theme } = userSettings;

// Spread Operator for immutable update
const initialState = { loading: false, data: null };
const loadingState = { ...initialState, loading: true };

// ====================================================================
// 9️⃣ Modules: Import/export and module organization
// ====================================================================

/*
 * الـ Modules هي آلية TypeScript/ES6 لتقسيم الكود وتنظيمه، وهي تدعم الـ Angular Architecture بشكل كامل.
 *
 * 1. Import/Export:
 * كل ملف TypeScript يُعتبر وحدة نمطية (Module) خاصة به. الـ 'export' تحدد العناصر التي يجب مشاركتها، والـ 'import' تحدد العناصر التي سيتم استهلاكها.[14]
 *
 * 2. التنظيم المعماري:
 * في Angular، يعد التنظيم المعتمد على الموديولات أمراً حيوياً لتحديد حدود الميزات أو نطاقات العمل (Domains) في التطبيق.[15]
 *
 * 💡 الـ Modules كعامل لتمكين الـ Lazy Loading:
 * التنظيم الواضح للـ Modules باستخدام `import`/`export` يُمكّن مجمع الكود (Bundler) من فصل أجزاء معينة من التطبيق إلى حزم منفصلة. هذا الفصل هو المتطلب الأساسي لتطبيق خاصية التحميل الكسول (Lazy Loading)، والتي تقلل بشكل كبير من حجم الحزمة الابتدائية المحملة عند بدء تشغيل التطبيق، مما يحسن من أداء المستخدم.[1]
 *
 * 💡 ملفات `index.ts` للواجهة:
 * لتجنب استيراد تفاصيل تنفيذية داخلية، يمكن للمطورين إنشاء ملف `index.ts` في جذر كل موديول (أو مجلد). هذا الملف يعمل كواجهة أمامية (Facade)، حيث يقوم بإعادة تصدير جميع العناصر العامة والمصرح بها للاستخدام الخارجي. هذه الممارسة تحافظ على نظافة الاستيرادات وتقلل من التعقيد في الموديولات المستهلكة.
 */
// --- file: models/user.ts ---
export interface User { id: number; }

// --- file: features/user/index.ts ---
// إعادة تصدير نموذج المستخدم
// export * from '../../models/user';

// --- file: app.component.ts ---
// استيراد نظيف عبر ملف الواجهة index.ts
// import { User } from './features/user';

// ====================================================================
// 🔟 TypeScript Configuration: Understanding tsconfig.json settings
// ====================================================================

/*
 * ملف 'tsconfig.json' هو قلب بيئة عمل TypeScript. يحدد هذا الملف جميع خيارات المترجم (Compiler Options) والملفات الجذرية للمشروع.[16]
 *
 * 1. التكوينات المتعددة في Angular:
 * مشاريع Angular تستخدم عادةً تسلسلاً هرمياً لملفات `tsconfig.json`. يتم فصل الإعدادات بين كود التطبيق (`tsconfig.app.json`) وملفات الاختبار (`tsconfig.spec.json`).[17] هذا الفصل ضروري للسماح بإضافة أنواع خاصة بالاختبار (مثل 'jasmine' أو 'chai') إلى بيئة الاختبار فقط، دون إبطاء الترجمة أو خلط الأنواع في بيئة التطبيق الأساسية.[17]
 *
 * 2. الخيارات الحاسمة:
 * - 'target': يحدد إصدار ES المستهدف (يجب أن يكون حديثاً مثل ES2020 أو أحدث).
 * - 'experimentalDecorators': يجب تعيينه على 'true' لدعم جميع Decorators التي يستخدمها Angular.[7]
 * - 'module': يحدد نظام الموديولات المناسب لعملية الـ Bundling.
 * - 'strict': تفعيل خيار التحقق الصارم.
 *
 * 💡 فرض الانضباط النوعي (Strict Mode):
 * يُعتبر تعيين 'strict: true' ممارسة معيارية وإلزامية في مشاريع Angular الحديثة لفرض أعلى مستوى من التدقيق النوعي.[1] هذا يضمن التعامل الصريح والآمن مع جميع الأنواع، ويقلل بشكل فعال من الأخطاء التي تظهر في وقت التشغيل (Runtime) نتيجة للقيم `null` أو `undefined`.
 */

// مثال على جزء من tsconfig.json يوضح خيارات الجودة:

/*
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "ES2020",
    "strict": true,
    "esModuleInterop": true,
    "experimentalDecorators": true,
    // يجب إدارة 'types' بعناية، وإضافتها في ملفات التكوين الخاصة (مثل spec) [17]
    "typeRoots": ["node_modules/@types"],
    "lib": ["es2020", "dom"]
  }
}
*/

/*
 * ====================================================================
 * الخلاصة وماذا تعلمنا؟
 * ====================================================================
 */

/*
 * لقد أثبتنا أن إتقان مفاهيم TypeScript الأساسية ليس مجرد ميزة إضافية، بل هو شرط أساسي لبناء تطبيقات Angular قوية وموثوقة. ترتبط جميع المفاهيم التي تمت دراستها ارتباطاً وثيقاً بالبنية المعمارية لـ Angular:
 * - الكلاسات والـ Decorators تشكل قلب نظام حقن التبعية ونظام الميتا داتا.
 * - الـ Generics وأنظمة الأنواع المتقدمة تضمن السلامة النوعية في طبقة البيانات وتزيد من قابلية إعادة الاستخدام.
 * - مفاهيم ES6+ تفرض ممارسات البرمجة الحديثة (مثل الثبات وتجنب فقدان سياق 'this').
 * - التكوين عبر 'tsconfig.json' يضمن أن المترجم يفرض أعلى معايير الجودة الممكنة.
 */

/*
 * ====================================================================
 * 🎯 Best Practices (أفضل الممارسات)
 * ====================================================================
 */

/*
 * 1. الالتزام الصارم بالأنواع (Strict Typing):
 * - ✅ تفعيل 'strict: true' في tsconfig.json هو نقطة البداية لضمان جودة الكود.[1]
 * - ✅ استخدم الـ Type Guards (typeof, instanceof) للتعامل الآمن مع Union Types وقيم الـ null/undefined.
 *
 * 2. عزل واجهة المكون (Component API Cleanliness):
 * - ✅ استخدم 'protected' للأعضاء التي تُستخدم فقط في الـ Template، للحفاظ على واجهة Component نظيفة ومحددة.[6]
 *
 * 3. تفضيل الثبات (Prefer Immutability):
 * - ✅ استخدم معامل النشر (Spread Operator) و 'readonly' للحفاظ على ثبات كائنات الحالة قدر الإمكان، مما يحسن من أداء كشف التغييرات في Angular.[3, 13]
 *
 * 4. استخدام الـ Generics لتجنب التكرار:
 * - ✅ صمم خدمات واجهات عامة (Generic Services/Interfaces) لزيادة كفاءة الكود وتقليل التكرار.
 *
 * 5. تنظيم الموديولات والتحميل الكسول:
 * - ✅ استخدم 'import'/ 'export' لتحديد حدود الموديولات بوضوح، مما يدعم الـ Lazy Loading ويحسن من الأداء العام للتطبيق.
 *
 * 6. استخدام 'async/await' لمعالجة العمليات المتسلسلة:
 * - ✅ تبسيط العمليات غير المتزامنة المعقدة باستخدام 'async/await' وتوحيد معالجة الأخطاء باستخدام `try/catch`.[12]
 */